## Manifest menedżera komponentów ESP-IDF.
## Zależność cJSON jest pobierana automatycznie przy pierwszej kompilacji
## (do katalogu managed_components/, który jest ignorowany przez git).
dependencies:
  idf:
    version: '>=4.1.0'
  espressif/cjson: ^1.7.19~2
